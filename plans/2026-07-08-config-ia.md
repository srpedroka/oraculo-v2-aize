# Plano: Configuracao de IA — validacao ao salvar + tom/persona por empresa

> Design gerado por auditoria multi-agente (2026-07-08), grounded no codigo real. Parte do pacote de melhorias — ver `plans/2026-07-08-pacote-melhorias.md`.

Tenho o contexto completo do código real. Segue o design.

---

# Melhorias na Configuração de IA — design fatiado

## Diagnóstico confirmado no código (base do desenho)

Rastreei o caminho `daily` de ponta a ponta e os dois problemas do enunciado se confirmam no código, não em suposição:

1. **Salva "ok" e só quebra no uso.** `supabase/functions/save-ai-settings/index.ts` grava `ai_function_settings` / `ai_model_keys` / `ai_provider_key_status` sem nunca tocar o provedor. Não há uma única chamada a `api.x.ai` no fluxo de salvar. O `grok-4.3` é aceito só porque existe no catálogo estático (`_shared/pricing.ts` linha 48), que é uma lista mantida à mão — não é verdade verificada contra a xAI.
2. **Erro de runtime é engolido em silêncio.** No `oracle-chat` (linhas ~150-175) o `callModel` está dentro de `try/catch` que cai em `fallbackReview`; no `whatsapp-webhook` (blocos de `callModel` em ~1418 e ~1608) o `catch` também descarta o erro. `model.ts` até monta um `Error` bom (`xAI/Grok não respondeu corretamente: <corpo do erro>`), mas ninguém persiste isso. O dono nunca vê o 401/404.
3. **UI nunca vê resultado.** Em `src/state/store.tsx` (linhas 1301-1331) as três ações de IA são `void callEdgeFunction(...).then(invalidateOrg)` — fire-and-forget. O `Settings.tsx` (`saveAiFunction`, linha 204) seta `aiMessage` otimista **antes** de qualquer confirmação. É literalmente impossível a tela dizer "salvou de verdade" ou "a chave é inválida".

Ou seja: `grok-4.3` inválido na xAI **ou** chave ruim produzem exatamente o sintoma observado (WhatsApp mudo, tela dizendo que salvou). As duas causas se resolvem com o mesmo mecanismo: **provar contra o provedor no momento de salvar** e **persistir o status do último uso**.

Padrões do projeto que vou reaproveitar (não inventar):
- RLS: `public.is_org_member(org_id)` lê, `public.is_owner(org_id)` escreve (visto em `20260704110000` e `20260704123000`). Grants para `authenticated` + `service_role`.
- Segredos (`ai_model_keys`) ficam em tabela `revoke all from anon, authenticated`, só `service_role`. Chave nunca volta pro cliente; UI só vê `key_preview` (`****1234`).
- Prompt é montado por array `.filter(Boolean).join("\n\n")` em `oracle-chat`, `whatsapp-webhook` (3 pontos) e `session-engine` (4 pontos), sempre começando por `PERSONA_ORACULO`.

---

# FATIA A — Validação ao salvar + confirmação real + status por função (itens 2.1 + 2.2)

Quebrada em 5 sub-fatias pequenas e testáveis. A0→A2 já resolvem o bug; A3→A4 dão visibilidade e polimento.

## A0 — Probe de modelo (helper server-side puro)

O tijolo de baixo, testável isolado, sem tocar banco nem UI.

**Arquivos**
- Novo: `supabase/functions/_shared/model-probe.ts`
- Usa: `supabase/functions/_shared/model.ts` (mesmas URLs/headers já existentes)

**Assinatura**
```ts
export type ProbeStatus =
  | "ok" | "invalid_key" | "unknown_model"
  | "rate_limited" | "provider_error" | "timeout" | "no_key";

export interface ProbeResult { status: ProbeStatus; httpStatus?: number; detail: string; }

export async function probeModel(
  provider: Provider, model: string, apiKey: string,
): Promise<ProbeResult>;
```

**Passos**
1. Chamada mínima: `messages:[{role:"user",content:"ping"}]`, `max_tokens: 1` (Anthropic `max_tokens: 1`; OpenAI Responses `max_output_tokens: 16`), `temperature: 0`, reaproveitando `fetchWithTimeout` (timeout curto, ex. 10s).
2. Mapear a resposta para `ProbeStatus`:
   - `200` → `ok`
   - `401 / 403` → `invalid_key`
   - `404`, ou `400` cujo corpo contenha `model`/`does not exist`/`unknown model` → `unknown_model`
   - `429` → `rate_limited`
   - `AbortError` → `timeout`
   - resto → `provider_error`
3. `detail`: mensagem do provedor **truncada a ~300 chars** e higienizada (nunca ecoar `apiKey`; a chave não vai no corpo de erro, mas trunco por garantia).

**Critérios de aceite**
- Dado `xai`+`grok-4.3`+chave real → retorna `ok` **ou** `unknown_model` (é isto que responde empiricamente se `grok-4.3` existe — sem eu chutar).
- Dado qualquer provider + chave lixo → `invalid_key`.
- Nunca lança; sempre resolve um `ProbeResult`. Custo ≤ ~1-16 tokens por probe.

## A1 — Modelo de dados: status por função e por chave

Menos invasivo: **estender as duas tabelas que já existem** em vez de criar novas (uma linha por função e uma por provider já existem).

**Migration nova** `supabase/migrations/20260708140000_ai_config_health.sql`
```sql
-- Status do último check por função (config-time e runtime)
alter table public.ai_function_settings
  add column if not exists last_status text
    check (last_status in ('ok','invalid_key','unknown_model','rate_limited','provider_error','timeout','untested')),
  add column if not exists last_status_detail text,
  add column if not exists last_status_source text
    check (last_status_source in ('save','manual','runtime')),
  add column if not exists last_checked_at timestamptz;

-- Validade da chave por provider
alter table public.ai_provider_key_status
  add column if not exists last_status text
    check (last_status in ('ok','invalid_key','unknown_model','rate_limited','provider_error','timeout','untested')),
  add column if not exists last_status_detail text,
  add column if not exists last_checked_at timestamptz;
```

**RLS / grants**: nada a mudar. As policies `ai_function_settings_read_org_member` / `_write_owner` e `ai_provider_key_status_read_org_member` / `_write_owner` já cobrem as colunas novas. Escritas de runtime vêm de `service_role` (bypassa RLS). `last_status_detail` **nunca** contém chave (garantido em A0).

**Decisão de design**: não crio tabela `ai_health_checks` separada — o requisito "ver status/erro por função" mapeia 1:1 numa coluna da linha que já existe por função. Se no futuro quiser histórico, aí sim vira tabela append-only (flagado abaixo).

**Critérios de aceite**
- `pnpm run lint`/build seguem limpos; migration idempotente (`add column if not exists`).
- Colunas visíveis via a query existente `select("*")` do store (linhas 765/776 do `store.tsx`) sem mudar a query.

## A2 — save-ai-settings valida e persiste (Edge Function)

**Arquivo**: `supabase/functions/save-ai-settings/index.ts`

**Contrato novo (importante para a UX)**
- Salvar continua; **falha de validação NÃO é HTTP 400**. Retorno `200` com corpo:
  ```json
  { "ok": true, "keyPreview": "****ABCD",
    "validation": { "scope": "daily", "status": "unknown_model",
                    "detail": "The model grok-4.3 does not exist",
                    "checkedAt": "2026-07-08T..." } }
  ```
- HTTP `4xx` fica só para falha real de salvar (sessão, `assertOwner`, input inválido). Isso importa porque `supabase-js` transforma não-2xx em `error` e some com o `data`. Validação precisa chegar como `data.validation`.

**Passos**
1. Após o upsert de função (bloco linhas 60-75) **ou** de chave (35-58), resolver a chave efetiva do provider em `ai_model_keys` (já é `service_role`).
2. Rodar `probeModel(provider, modelEfetivo, apiKey)`.
   - Salvando função: modelo = o que está sendo salvo. 
   - Salvando só chave (fluxo linhas 78-88, sem `model`): usar o modelo já configurado para alguma função desse provider; se nenhuma, **modelo default por provider** (mesmo mapa do `Settings.tsx` `DEFAULT_MODEL_BY_PROVIDER`). Se não houver chave ainda → `status:"no_key"`, sem probe.
3. Persistir: `last_status/last_status_detail/last_checked_at`, `last_status_source:"save"` em `ai_function_settings` (na função) e/ou `ai_provider_key_status` (no provider).
4. Aceitar `validate?: boolean` no body (default `true`) para permitir um botão "Testar agora" que **só valida** (`mode:"test"`, sem gravar) reusando o mesmo probe.

**Critérios de aceite**
- Salvar `daily = xai/grok-4.3` com chave inválida → `200 { validation.status:"invalid_key" }` e `ai_function_settings.last_status="invalid_key"`.
- Salvar com config boa → `validation.status:"ok"`.
- Chave nunca aparece no retorno nem em `detail`.
- `assertOwner` segue barrando não-donos (já existe, linha 27).

## A3 — Captura de status em runtime (fecha o "silêncio")

Centralizar as chamadas para que uma falha real de uso vire status visível — é o que teria exposto o bug atual.

**Arquivos**
- Novo helper `supabase/functions/_shared/call-for-function.ts`:
  ```ts
  export async function callModelForFunction(
    client, orgId, fn: AiFunction, aiRoute, systemPrompt, messages,
  ) {
    try {
      const result = await callModel(aiRoute.provider, aiRoute.model, aiRoute.apiKey, systemPrompt, messages, aiRoute.limits);
      await markFunctionStatus(client, orgId, fn, "ok", "runtime");
      return result;
    } catch (e) {
      await markFunctionStatus(client, orgId, fn, classify(e), "runtime", e.message);
      throw e; // fallback do caller continua igual
    }
  }
  ```
- Ajustar os call-sites para usar o wrapper: `oracle-chat/index.ts` (~linha 168), `whatsapp-webhook/index.ts` (blocos ~1418, ~1608, ~1781), `_shared/session-engine.ts` (4 pontos: 685, 808, 936, 1064). Mudança mecânica: trocar `callModel(...)` por `callModelForFunction(client, orgId, fn, aiRoute, ...)`.

**Critérios de aceite**
- Forçar um erro (modelo inválido) e mandar msg no WhatsApp → `ai_function_settings.daily.last_status` vira o código do erro com `source:"runtime"`, sem mudar o comportamento de fallback pro usuário final.
- Sucesso normal reseta `last_status="ok"`.

## A4 — UI: confirmação real + status por função + higiene de catálogo

**Arquivos**: `src/pages/Settings.tsx`, `src/state/store.tsx`, `src/types/index.ts`, `src/lib/aiPricing.ts`

**Store (tirar o fire-and-forget)**
- Adicionar métodos imperativos no contexto que **retornam** o payload, em vez de passar por `dispatch`:
  ```ts
  saveAiFunctionSetting(fn, provider, model): Promise<{ ok; validation }>
  saveAiProviderKey(provider, apiKey): Promise<{ ok; validation }>
  testAiFunction(fn): Promise<{ validation }>   // mode:"test"
  ```
  Cada um faz `await callEdgeFunction("save-ai-settings", ...)`, `invalidateOrg()`, e devolve `data`. (O `callEdgeFunction`, linha 492, já retorna `data`.)

**Settings.tsx**
- Estado por função: `idle | testing | ok | error` + mensagem. Ao salvar, `await` o método e traduzir `validation.status` em PT-BR claro:
  - `ok` → check verde "Validado com xAI/Grok agora."
  - `unknown_model` → "xAI não reconhece o modelo `grok-4.3`. Confira o id do modelo." 
  - `invalid_key` → "A chave xAI foi recusada (não autorizada). Revise a chave."
  - `timeout`/`provider_error` → "Não consegui falar com o provedor agora; tente de novo."
- Badge de saúde por função lendo `ai_function_settings.last_status` + `last_checked_at` (ex.: "Último uso: erro (modelo não encontrado) há 2h"), separando **erro de config** de **erro no último uso real** — é o "ver status/erro por função" pedido.
- Botão "Testar agora" por função e por chave → `testAiFunction`.
- Manter o cockpit limpo: badge discreto (verde/âmbar/vermelho) + texto curto, sem poluir.

**Higiene de catálogo / model ids válidos**
- O catálogo (`aiPricing.ts` + `pricing.ts`) deixa de ser **gate** e passa a ser só **pricing + sugestões** do `datalist`. A fonte de verdade de "esse id existe" passa a ser o probe (A0). Isso é o que impede o `grok-4.3` fantasma de voltar: mesmo que fique no catálogo, salvar mostra vermelho na hora.
- Deduplicação: hoje `supabase/functions/_shared/pricing.ts` e `src/lib/aiPricing.ts` são cópias que já divergem em risco. Proposta: uma fonte única `shared/model-catalog.json` na raiz, importada pelos dois runtimes (Vite via `import ... assert { type: "json" }`; Deno idem). **Flagado como decisão** — melhora manutenção mas não é necessário pro bug.

**Critérios de aceite (fatia A completa)**
- Reproduzir o cenário real (daily=xai/grok-4.3, chave 84 chars): ao salvar, a tela mostra **vermelho com o motivo exato** vindo da xAI, em vez de "salvo".
- Se a chave/modelo estiverem certos, mostra **verde "validado"**.
- Se o WhatsApp quebrar em produção, o dono vê o status por função em Configurações sem abrir logs.
- Chave nunca trafega para o cliente; validação 100% server-side sob `assertOwner`.

---

# FATIA B — Tom/persona por empresa (item 2.3)

Não é segredo → RLS normal (membro lê, dono escreve). Quebrada em 3 sub-fatias.

## B1 — Modelo de dados

**Migration** `supabase/migrations/20260708150000_org_ai_tone.sql`
```sql
create table if not exists public.org_ai_tone (
  org_id uuid primary key references public.organizations(id) on delete cascade,
  preset text not null default 'equilibrado'
    check (preset in ('equilibrado','acido','gentil','direto','motivador','custom')),
  axis_acidity smallint not null default 0 check (axis_acidity between -2 and 2),  -- -2 gentil ↔ +2 ácido/franco
  axis_drive   smallint not null default 0 check (axis_drive   between -2 and 2),  -- -2 direto/seco ↔ +2 motivador
  custom_note text check (custom_note is null or char_length(custom_note) <= 280),
  updated_at timestamptz not null default now(),
  updated_by uuid references auth.users(id)
);

alter table public.org_ai_tone enable row level security;
grant select, insert, update, delete on public.org_ai_tone to authenticated;
grant select, insert, update, delete on public.org_ai_tone to service_role;

drop policy if exists org_ai_tone_read_org_member on public.org_ai_tone;
create policy org_ai_tone_read_org_member on public.org_ai_tone
  for select to authenticated using (public.is_org_member(org_id));

drop policy if exists org_ai_tone_write_owner on public.org_ai_tone;
create policy org_ai_tone_write_owner on public.org_ai_tone
  for all to authenticated using (public.is_owner(org_id)) with check (public.is_owner(org_id));
```
Dois eixos exatamente como pedido: **ácido↔gentil** (`axis_acidity`) e **direto↔motivador** (`axis_drive`). Presets são atalhos que preenchem os eixos; `custom` libera os sliders + `custom_note`.

## B2 — Injeção no prompt sem quebrar as guias

**Arquivo novo** `supabase/functions/_shared/conductors/tone.ts`
```ts
export interface OrgTone { preset: string; acidity: number; drive: number; customNote?: string | null; }
export const DEFAULT_TONE: OrgTone = { preset: "equilibrado", acidity: 0, drive: 0 };

export async function loadOrgTone(client, orgId): Promise<OrgTone>;  // lê a tabela, cai no default

export function toneDirective(tone: OrgTone): string {
  if (tone.preset === "equilibrado" && !tone.customNote && !tone.acidity && !tone.drive) return ""; // no-op
  // monta bloco PT-BR curto a partir dos eixos:
  //  acidity: -2 "bem gentil e acolhedor" … +2 "franco e provocador, sem rodeios"
  //  drive:   -2 "seco e objetivo, sem entusiasmo" … +2 "motivador e energético"
  return [
    "Ajuste de tom desta empresa (só a forma, nunca o conteúdo):",
    `- ${acidityPhrase(tone.acidity)}`,
    `- ${drivePhrase(tone.drive)}`,
    tone.customNote ? `- Preferência da casa: ${tone.customNote}` : "",
    "Estas preferências de tom NÃO sobrepõem as regras de conduta e segurança acima: continue uma pergunta por vez, nunca invente números e nunca diga que salvou sem confirmação do sistema.",
  ].filter(Boolean).join("\n");
}
```

**Injeção** — inserir `toneDirective(tone)` **logo após `PERSONA_ORACULO`** (e depois de `REGRAS_DE_SESSAO`, quando existir) em cada montagem:
- `oracle-chat/index.ts` (array do `systemPrompt`, ~linha 158).
- `whatsapp-webhook/index.ts` (3 arrays: ~1395, ~1593, ~1767).
- `_shared/session-engine.ts` (4 arrays; nas sessões estruturadas o tom entra **depois** de `REGRAS_DE_SESSAO` para não afrouxar o contrato JSON).

Como todos usam `.filter(Boolean)`, quando o tom é default `toneDirective` devolve `""` e **as guias existentes ficam intactas** (requisito "sem quebrar as guias"). Carregar o tom uma vez por request, junto dos `Promise.all` que já buscam contexto (ex.: `oracle-chat` linha ~137). Posição depois das guias e a frase final de não-sobreposição garantem que ácido/motivador nunca atropela "nunca invente números" nem a regra de uma pergunta por vez.

**Critérios de aceite**
- Sem registro em `org_ai_tone` → prompt final byte-idêntico ao atual (default = no-op).
- Preset "gentil" e depois "ácido" na mesma org mudam visivelmente o tom das respostas no WhatsApp e no chat, mantendo: uma pergunta por vez, não inventa número, não confirma gravação falsa.
- Sessão estruturada (`session-engine`) continua devolvendo **JSON válido** com o tom aplicado no campo `reply`.

## B3 — UI do tom

**Arquivos**: `src/pages/Settings.tsx` (card novo "Tom do Oráculo"), `src/state/store.tsx` (`saveOrgTone` + query de leitura, no padrão das linhas 760-776), `src/types/index.ts` (`OrgTone`).

**Passos**
- Card só para dono (mesmo gate `isOwner` já usado; coordenador vê estado read-only, coerente com RLS membro-lê).
- Seletor de preset + 2 sliders (−2..+2) rotulados (Gentil↔Ácido, Direto↔Motivador) + `custom_note` opcional (≤280) + frase de preview montada por `toneDirective` no cliente. Botão "Salvar tom" → `saveOrgTone` (awaited, com confirmação real reaproveitando o padrão da Fatia A).
- Realtime: adicionar `org_ai_tone` ao bloco de `invalidateOrg` (store linhas 865-866) se quiser refletir na hora.

**Critérios de aceite**
- Dono altera e salva → confirmação de verdade; recarregar mantém o valor.
- Coordenador vê mas não edita; tentativa de escrita é barrada pela RLS.
- `pnpm run lint && pnpm run build` limpos.

---

# Ordem sugerida de implementação (cada passo entrega valor testável)

1. **A0 + A1 + A2** → o bug fica visível ao salvar (maior ROI, resolve o incidente).
2. **A3** → deixa de engolir erro de runtime.
3. **A4** → confirmação e badges na UI + catálogo vira sugestão, não gate.
4. **B1 → B2 → B3** → tom por empresa, isolado do resto.

---

# Decisões de produto em aberto (flagadas, não decididas por mim)

**Fatia A**
- **Validar em todo save vs botão "Testar"**: recomendo validar no save (é o pedido) **e** expor "Testar agora"; custo é ~1-16 tokens e ~1 chamada. Confirmar se a latência extra no salvar é aceitável.
- **Modelo usado ao validar só a chave** (fluxo `upsert_ai_provider_key`, sem modelo): usar o modelo já configurado para o provider, ou um default por provider, ou não validar até salvar uma função? Recomendo "modelo configurado, senão default".
- **Bloquear ou só avisar** quando `daily` fica inválido: recomendo salvar-com-aviso vermelho (o dono pode estar prestes a corrigir a chave). Confirmar.
- **`grok-4.3` é o id real da xAI?** Não dá para afirmar daqui — o probe (A0) responde empíricamente na primeira execução. Decidir se corrige o catálogo agora ou espera o probe apontar.
- **Unificar catálogo** (`pricing.ts` vs `aiPricing.ts`) numa fonte JSON única: melhora manutenção, não é necessário pro bug. Fazer agora ou depois?
- **Histórico de status**: hoje guardo só o último (uma coluna). Se quiserem timeline de falhas, vira tabela append-only `ai_health_checks` — flag para depois.

**Fatia B**
- **Rótulo "ácido"** num cockpit executivo: confirmar se está on-brand ou renomear para "franco"/"provocador". A frase de guardrail já impede grosseria com o usuário — confirmar o teto de "quão ácido" é aceitável.
- **Escopo do tom**: por empresa (recomendado no primeiro corte) vs também por função/canal (ex.: WhatsApp mais leve, planejamento mais formal). Decidir se `daily`/`planning` podem ter tons distintos.
- **Visibilidade para coordenador**: RLS deixa membro ler. Confirmar se o coordenador deve enxergar o tom configurado ou se é só-dono também na leitura.

---

**Arquivos que este design cria ou altera** (todos absolutos):
- Novos: `/Users/luisguilherme/Documents/Oraculo/supabase/functions/_shared/model-probe.ts`, `/Users/luisguilherme/Documents/Oraculo/supabase/functions/_shared/call-for-function.ts`, `/Users/luisguilherme/Documents/Oraculo/supabase/functions/_shared/conductors/tone.ts`, migrations `.../supabase/migrations/20260708140000_ai_config_health.sql` e `.../20260708150000_org_ai_tone.sql`, e (opcional) `/Users/luisguilherme/Documents/Oraculo/shared/model-catalog.json`.
- Alterados: `.../supabase/functions/save-ai-settings/index.ts`, `.../oracle-chat/index.ts`, `.../whatsapp-webhook/index.ts`, `.../_shared/session-engine.ts`, `/Users/luisguilherme/Documents/Oraculo/src/state/store.tsx`, `/Users/luisguilherme/Documents/Oraculo/src/pages/Settings.tsx`, `/Users/luisguilherme/Documents/Oraculo/src/types/index.ts`, e (se unificar catálogo) `.../_shared/pricing.ts` + `/Users/luisguilherme/Documents/Oraculo/src/lib/aiPricing.ts`.
